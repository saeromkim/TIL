# 강의 전 숙제(어떤 방법으로 풀건지 생각해보기)
### 다이나믹 프로그래밍 1.5
#### RGB거리
- d[i][j] = i번 집을 j로 칠했을 때 1~i번 집을 칠하는데 드는 비용의 최솟값
- i가 빨강일때, 파랑일때, 초록일때 3가지 경우를 나눠서 생각한다.
- d[i][0] = min(d[i-1][1],d[i-1][2]) + a[i][0]
- d[i][1] = min(d[i-1][0],d[i-1][2]) + a[i][1]
- d[i][2] = min(d[i-1][3],d[i-1][1]) + a[i][2]
#### 정수 삼각형
- d[i][j] = i행 j열로 오는 최대 합
- d[i][j] = max(d[i-1][j], d[i-1][j-1])
#### 동물원
- d[i][j] = i번째 행의 i번째 칸에 사자가 있을 때 1~i행까지 배치하는 경우의 수
- j=0 배치하지 않음, j=1 첫번째 칸에 배치 j=2 두번째 칸에 배치
- d[0][0] = 1 사자를 한마리도 배치하지 않은 경우
- 세가지 경우에 따라 그 전 칸의 배치 경우 수를 모두 더한다.
- d[i][0] = d[i-1][0] + d[i-1][1] + d[i-1][2]; //한마리도 배치하지 않으면 그 전칸에는 세가지 경우 모두 가능
- d[i][1] = d[i-1][2] + d[i-1][0]; 
- d[i][2] = d[i-1][0] + d[i-1][1];
#### 극장 좌석
- 고정좌석을 기준으로 좌석을 나눈다음, 각각 다이나믹한 결과를 모두 곱해준다.
#### 상자 넣기
- d[i] = i번째 상자까지 있을 때 i번째에 들어갈 수 있는 상자의 개수의 최댓값
- i번째를 제외한 상자들 중 i보다 큰 숫자들 중 가장 작은 숫자를 선택해야 한다.
- j: i보다 순서가 앞에 있고, 숫자도 더 작은 상자 중에 d[j]가 가장 큰 값
- d[i] = max(d[j]) + 1
### 다이나믹 프로그래밍 2.5
#### 소형 기관차
- d[i][j] = i번 사람까지 있을 때, j개의 소형 기관차로 운송할 수 있는 최대 손님의 수
- d[i][j] = max(d[i-1][j],d[i-m][j]) + (a[i-m+1] ~ a[i] 까지 합)
#### 박스 안의 열쇠
- 폭탄으로 박스를 연다->열쇠로 박스를 연다
- i번째 수가 기존에 있던 사이클에 들어가는 경우
- d[i-1][j] * (i-1) 음
#### 팔굽혀펴기
- d[i][j] = 총 팔굽혀펴기를 i번했고, 이 때 점수 j가 가능한가?

- 1.5, 2.5슬라이드는 급하게쓰는 슬라이드 ㅇㅅㅇ
- 기타는 설명하기 애매한 애들
- bfs2는 bfs1보다 조금 어려운 bfs
- (참고용)은 급하지 않은 내용이라..
(수업시작)
# 기하 알고리즘.ppt
- 젤 중요한건 다각형인데 모든 기하알고리즘의 중요한 것은 방향을 알아내는 것.
- 그걸 알아내는 것 점 세 개의 방향성을 알아내는게 ccw이다
- 외적을 이용해서 구할 수 있다
- 세 점이 일직선 상에 있는 경우는 없다 라는 말이 있으면 어려워짐
- 기하는 예외처리가 관건인데, 이 경우 예외가 엄청나게 발생함
- 쉬운 기하 문제는 예외를 생각해야하는 경우가 많지 않아서 그냥 ccw만 해주면 된다.
## 다각형의 내부/외부
- 바깥에 임의의 점을 하나 찍고, 그 점과 주어진 점을 이었을때, 교점의 개수가 짝수면 외부, 홀수면 내부.
- 어마어마한 예외발생
## 삼각형의 넓이
- 외적을 이용함
- 막상 식을 구하면 26w 코드 디게 간단해
(개졸리다)
## 블록 껍질 ( Olnlo)
- 모든 다각형으로 사용 가능
-  ssw를 한 결과가 반시계방;;
- (졸려) 
- 각도를 구할 수도 있음. 정수가 안나옴
- 스택 이용
- 눈뜨고 계속 꿈끄는 기분
### Line Sweep
-점이 막 잇는데 선분하나 긋고 훑으면서 쭉 보는 알고리즘
#### 가장 가까운 두 점(젤 어려움)
- 분할정복으로 푸는 문제
- O(n^2) 두점의 쌍을 구하는 거니 모든 쌍의 거리름 구함
- d보다 거리가 작아질 수 없다.
- 모든 점을 조사하는 것처럼 보이지만 정답이 될 수 있는 후보를 줄이기 때문에
- 거리의 차이가 d가 나오게 다 만들어보면 후보점이 6개다. 이것보다 많이 나올 수 없다.
- 개어렵넹
- 최대 검사할 수 있는 점의 후보가 6개까지 나온다는 뜻이당.
- 분할정복으로
- 우리가 하려는거랑 관련있는 건 아니라서 그냥 넘어갈게요....(응?)
#### 겹치는 선분
- 회의실이랑 비슷!
-(녜?) 문제 풀이는 간단. 시작이랑 끝을 1,-1로 넣고 정렬한다음 계속 더하면됨
#### 선긋기
- 겹치는 선분이랑 똑같이 풀 수 있음

### 다이나믹 3 ppt
#### 가장 큰 정사각형
- 길이가 가장 작은거 +1을 해야함
- 직사각형이면 변의 길이를 두개 구해야하기 때문에 이렇게 못 푼다
#### (히스토그램)최대 직사각형
- 밑변이 어디에 있나를 본다.
- 히스토그램 문제를 응용해서 푼다.
- 행렬에서 가장 큰 직사각형 찾기
- 밑에서부터 1이 연속되는지 체크
- 풀이가 많은 문제
# 0228.ppt
- 질문게시판에 코테커리큘럼
#### 로마 숫자 만들기
- 숫자 조합에서 순서는 의미가 없으므로 4^20에서 n^4까지 줄일 수 있다.
- n^3까지 줄일 수 있는데, 변수 하나를 n-a-b-c로 표현하면 됨
#### 뱀과 사다리 게임
- 그냥 bfs 뱀이냐 사다리냐에 따라서 이동하는 거. 그렇게 복잡한 코드는 아님
- 지름길이라는 문제가 있는데 이거랑 비슷하다, dfs,dp로 풀 수 있다
#### Two Dots
- 4개 이상 
#### 겉넓이 구하기
#### 모양만들기
- 연결되어 있는 1의 최대 개수 
- o(nm)만에 해결할 수 있다. 먼저 모양을 찾고 각각의 수에 대해서 따져본다.
- dfs, bfs로 풀 수 있다. 그냥 푼다는 거는 보통 부르트포스
- 제한에 따라서 문제풀이가 많이 바뀌는데 이 문제가 대표적
- 최대 최소 구할 때 팁??
- 스티커 문제. 어차피 정답이 d[n]이라는 걸 알아도 불안하면 그냥 전체의 최댓값 for문돌려서 max값 구해도 된당
- 동적할당 할필요없음. 두달동안 문제풀면서 한 적도 없지만..
#### 나3곱2
- 연산의 특징 이용
- 3이 몇개 들어가 있는지 확인 후, 그걸 기준으로 나눈 후 다 더하면 됨.
#### 두 스티커
- 붙여진 스티커 넓이 최댓값.
- n개 중에 2개를 골라서 붙이는 문제. +) 회전 조건 등을 더함
#### 2*2*2 큐브
- 큐브를 한번 돌려서 풀 수 있는지 ?
- 다 구해보면 됨. 회전시키는 경우 여러개(12)
- 말그대로 구현하는 문제.
- 큐브를 풀라는 문제는 안나온다 경우의 수가 너무 많기 때문에..
#### 서울 지하철 2호선
- 이 문제의 핵심은 순환선을 찾는 것
- 순환선이 어딘지 알면 bfs(시작점을 순환섬을 지나는 정점으로 두면 됨)
- 순환선 찾는 거는 Two dots 문제에서 했잖아
- 구현은 간단하지 않다..
- dfs하다가 이미 방문한 정점을 만나면 그때부터 사이클을 입력해서 찾는당
#### 움직이는 미로 탈출
- 8 * 8 체스판 그냥 미로탈출이면 dfs,bfs로 갈 수 있냐 없냐만 해서 구하면 되는데..
- 벽이 내려오니까 어려워지는 문제
- 1초후,2초후...8초후 지도 8가지 + 벽이 다 내려왔을 때 지도 1가지 해서 총 9가지 
#### 레벨 햄버거
- 재귀로~ 재밌음

- 나머지는 다 비슷한 수준의 문제입니다
